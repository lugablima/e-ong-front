import { Text } from "@chakra-ui/react";

export default function SignInPage() {
  return (
    <Text fontFamily="default" fontSize={100} fontWeight={700}>
      Eu sou a sign in page!
    </Text>
  );
}
